# عزل المتاجر (Multi-Tenant Security) — دليل مختصر

## ما تغيّر
- تسجيل دخول حقيقي لكل متجر (بريد/كلمة سر) بدل الدخول المجهول.
- كل بيانات القاعدة أصبحت تحت `stores/{storeId}/...` بدل الجذر المشترك.
- `storeId` يُشتق فقط من `custom claim` الموجودة في توكن Firebase الموثّق من
  السيرفر — لا يمكن للمستخدم تزويرها من المتصفح.
- `database.rules.json` يفرض `auth.token.storeId === $storeId` على كل قراءة/كتابة.
- `api/send-to-all.js` يتحقق من هوية المتصل عبر `verifyIdToken` ويحصر الإشعارات
  في توكنات متجره فقط.
- مفاتيح `localStorage` الخاصة بالمنتجات/المخزون أصبحت مربوطة بـ storeId
  (حماية إضافية عند استخدام نفس الجهاز لأكثر من متجر) — المخزون بقي محليًا
  كما هو، لم يُنقل لـ Firebase.

## خطوات يجب أن تقوم بها أنت (ممنوع عليّ الـ Deploy)
1. **الصق `database.rules.json`** في Firebase Console → Realtime Database → Rules، واضغط نشر.
2. **أنشئ حسابات المتاجر**:
   ```
   npm install
   node scripts/create-store.js create \
     --service-account ./serviceAccountKey.json \
     --database-url https://tajirapp-fe79d-default-rtdb.firebaseio.com \
     --store-id store_default \
     --email your-store@example.com \
     --password "كلمة سر قوية"
   ```
3. **ارفع الملفات** (`index.html`, `api/send-to-all.js`) لمكان الاستضافة الحالي بنفسك.

## الاختبارات
- `node tests/inventory-logic.test.js` — **تم تشغيله فعليًا هنا، 16/16 ناجح.**
  يغطي منطق المخزون (غير المُعدَّل) + عزل مفاتيح localStorage الجديد.
- `tests/security-rules.test.js` — اختبار قواعد الأمان الحقيقي (عزل السيرفر،
  محاولات تغيير store_id). لم يُشغَّل هنا (لا يوجد إنترنت/Firebase CLI في بيئة
  التنفيذ). شغّله عندك:
  ```
  npm install
  npm run test:rules
  ```

## فحص يدوي موصى به قبل الاعتماد النهائي (متصفح حقيقي)
- تسجيل الدخول بحساب متجر A ثم محاولة الوصول لبيانات متجر B عبر تعديل الرابط/الكونسول يدويًا.
- تسجيل خروج ودخول بمتجر مختلف على نفس الجهاز، والتأكد أن قسم المنتجات فارغ (لا يرى بيانات المتجر السابق).
- التأكد أن وظائف المخزون (إضافة/خصم/بحث/تنبيه النفاد) تعمل كالمعتاد.
- إرسال إشعار جماعي من حساب متجر A والتأكد أنه يصل فقط لهواتف مسجَّلة بمتجر A.

---

## تحديث أمني: الإرسال الجماعي وإنشاء الحسابات والخروج

**1) الإرسال الجماعي (`api/send-to-all.js`) — الحماية فالسيرفر فقط**
- ما بقى حتى رمز ولا بصمة (hash) فالواجهة (`index.html`) ولا فالمستودع.
- ضع الرمز فقط كمتغير بيئة فـ Vercel باسم `BROADCAST_PIN` (نوع **Sensitive**، **8 خانات أو أكثر**، والأفضل أرقام طويلة أو جملة). إذا كان مفقودًا أو أقصر من 8 خانات، السيرفر يرفض الإرسال (503).
- **الرمز القديم (4 خانات) مكشوف** (بصمته كانت عامة) — لا تستعمله ولا تعيد استعماله.
- 5 محاولات خاطئة لكل حساب فـ 15 دقيقة ثم قفل مؤقت (429). العدّاد يُخزَّن فـ `_security/broadcast/{uid}` (وصول Admin SDK فقط؛ قواعد الجذر مغلقة على العملاء).
- متغيرات Vercel المطلوبة: `FIREBASE_SERVICE_ACCOUNT` و`FIREBASE_DATABASE_URL` و`BROADCAST_PIN`.

**2) إنشاء حسابات التجار — System Admin فقط**
- `SETUP_KEY` أُلغي نهائيًا: **احذفه من Vercel إن كان موجودًا**. `api/setup-store.js` صار `POST` فقط ويشترط ID Token لحساب عنده `systemAdmin` (تُمنح فقط عبر `scripts/create-admin.js`).
- الحساب المُنشأ يأخذ `{ storeId }` فقط. الطريقة المعتمدة حاليًا لإنشاء الحسابات هي `scripts/create-store.js` من جهاز عندك (Cloud Shell أو كمبيوتر) لأن لوحة الأدمن لم تُبنَ بعد.

**3) تعطيل التسجيل الذاتي (إجراء يدوي فـ Firebase Console — لا يمكن فرضه من الكود)**
- Authentication ← Settings ← User actions ← ألغِ **Enable create (sign-up)**، ثم احفظ. إنشاء الحسابات عبر Admin SDK يستمر بلا مشكلة.

**4) تسجيل الخروج وتبديل المتجر**
- `storeLogout` يمسح توكن الجهاز من `stores/{storeId}/fcmTokens` ثم يبطّله (`deleteToken`) قبل `signOut`. وعند دخول متجر مختلف بعد جلسة انتهت بلا خروج، يُبطَّل التوكن القديم قبل تسجيل توكن جديد.

---

## لوحة System Admin (`admin.html` + `admin.js`)

**الوصول:** `https://<نطاق-المشروع-على-Vercel>/admin.html` — لازم تكون على **نفس النطاق** ديال الـ API (ما كاينش CORS فـ `setup-store` و`admin-merchants`). الصفحة ما مربوطة من تطبيق التاجر (`index.html`) ولا فيها زر يوصل ليها.

**من يدخلها:** فقط حساب عنده claim `systemAdmin === true` (يُنشأ بـ `node scripts/create-admin.js create ...`). حساب تاجر (storeId) يدخل ويتقابل بـ "هذا الحساب ليس System Admin" ويُسجَّل خروجه. **التحقق النهائي فالسيرفر**: كل طلب كيمشي بـ ID Token، والـ API كيتحقق (`checkRevoked`) ويشترط `systemAdmin` — فحص الواجهة للعرض فقط.

**ما تقدر تعمل:**
1. **إضافة تاجر**: الاسم + البريد + كلمة السر (8+ خانات، أو زر "توليد" لكلمة قوية 16 خانة) + storeId اختياري. إذا تركته فارغًا يُولَّد تلقائيًا (`store_` + 8 خانات) ويُرفض (409) إذا كان مستعملًا. بعد النجاح تظهر بطاقة بيانات الدخول **مرة واحدة** (كلمة السر ما كتتخزّن فأي مكان)، وتتمسح بزر "تم" أو بعد دقيقتين أو عند الخروج.
2. **عرض التجار**: الاسم، البريد، storeId (مع زر نسخ)، الحالة (نشط/معطّل)، آخر دخول، تاريخ الإنشاء. مع بحث وتصفية بالحالة وعدادات.
3. **تعطيل/تفعيل**: التعطيل = `disabled:true` + إبطال الـ refresh tokens. لا يمكن تعطيل نفسك ولا حساب System Admin ولا حساب بلا storeId.

**ملاحظات أمنية:**
- ID Token اللي عند التاجر المعطَّل يبقى صالحًا **حتى ساعة كحد أقصى** لقراءة/كتابة بياناته فـ Realtime Database (قواعد Firebase ما كتشوفش حالة `disabled`)، لكن ما بقاش يقدر يدخل ولا يجدد الجلسة، و`send-to-all` كيرفضو فورًا. إذا احتجت قطعًا فوريًا كاملًا: أضف فحصًا خاصًا فقواعد قاعدة البيانات (مرحلة لاحقة).
- ما كاينش حذف حسابات ولا تغيير كلمة سر تاجر من اللوحة (خارج نطاق هذه المرحلة).
- `database.rules.json` ما تغيّر: الواجهة ما كتلمس قاعدة البيانات، كل شيء عبر الـ API.
- متغيرات Vercel المطلوبة للـ API: `FIREBASE_SERVICE_ACCOUNT` و`FIREBASE_DATABASE_URL` (و`BROADCAST_PIN` للإرسال الجماعي). `SETUP_KEY` ما بقى مستعملًا.
- تسجيل الدخول فاللوحة كيستعمل جلسة SESSION (كتنتهي بإغلاق المتصفح). لا تفتح اللوحة من جهاز مشترك.

**تجربة يدوية بعد النشر (كل خطوة على الهاتف):**
1. افتح `/admin.html` وادخل بحساب System Admin.
2. جرّب الدخول بحساب تاجر: لازم يُرفض.
3. أضف تاجرًا بلا storeId، ثم بـ storeId مكرر (لازم 409).
4. ادخل بحساب التاجر الجديد فـ `index.html` وتأكد أنه يرى متجره فقط.
5. عطّل التاجر: لازم ما يقدرش يسجل دخول جديد. فعّله ورجّع الاختبار.
